import React from 'react'

import * as Styled from './style'

const Filters = ({ ...props }) => {
    return (
        <Styled.Container>
            Make something great with the Filters component here!
        </Styled.Container>
    )
}

export default Filters