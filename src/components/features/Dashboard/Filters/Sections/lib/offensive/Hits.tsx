import OffensiveRangeSelector from '../../OffensiveRangeSelector'

const Hits: React.FC = () => (
    <OffensiveRangeSelector
        title="H"
        filterKey="H"
        step={1}
    />
)

export default Hits